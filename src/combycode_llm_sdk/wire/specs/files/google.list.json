{
  "id": "google/files.list",
  "provider": "google",
  "api": "files",
  "envelope": {
    "method": "GET",
    "bodyKind": "none",
    "url": {
      "$join": [
        {
          "$config": "baseURL"
        },
        "/v1beta/files"
      ]
    },
    "headers": [
      {
        "name": "x-goog-api-key",
        "value": {
          "$config": "apiKey"
        }
      }
    ],
    "query": [
      {
        "name": "pageSize",
        "value": 100
      }
    ]
  },
  "_note": "Auth is the x-goog-api-key HEADER, never ?key= in the URL: a key in a query string is copied into every access log and telemetry span the request passes through."
}
