{
  "id": "google/media.download",
  "provider": "google",
  "api": "media",
  "_note": "Fetch generated bytes from a URI the operation result carries. The URI arrives WITH the key already on it - the caller adds it, because only the caller knows whether the operation supplied one - so the spec must not append a second.",
  "envelope": {
    "method": "GET",
    "bodyKind": "none",
    "url": {
      "$": "downloadUrl"
    },
    "headers": [
      {
        "name": "x-goog-api-key",
        "value": {
          "$config": "apiKey"
        }
      }
    ]
  }
}
