{
  "id": "google/media.operation.status",
  "provider": "google",
  "api": "media",
  "_note": "Veo returns a long-running operation; this polls it. `operationId` is the full resource name the submit call returned, so the path is just the version prefix plus that name. Auth is the x-goog-api-key HEADER, never ?key= in the URL: a key in a query string is copied into every access log and telemetry span the request passes through.",
  "envelope": {
    "method": "GET",
    "bodyKind": "none",
    "url": {
      "$join": [
        {
          "$config": "baseURL"
        },
        "/v1beta/",
        {
          "$": "operationId"
        }
      ]
    },
    "headers": [
      {
        "name": "x-goog-api-key",
        "value": {
          "$config": "apiKey"
        }
      }
    ]
  }
}
